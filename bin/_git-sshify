#compdef git-sshify
#autoload

# This adds git-sshify to the list of known git commands and provides completion
# for its options.

local -a _1st_arguments

_1st_arguments=($(git remote show))

_describe -t commands "git-sshify" _1st_arguments
